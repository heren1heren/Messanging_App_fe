import { FC } from 'react';
import Layout from './Layout';

type SignInProps = {};
export const SignIn: FC<SignInProps> = ({}) => {
  return (
    <Layout>
      <div>sign in page</div>
    </Layout>
  );
};
