import { FC } from 'react';
import Layout from './Layout';

type SignUpProps = {};
export const SignUp: FC<SignUpProps> = ({}) => {
  return (
    <Layout>
      <div>I am sign up page</div>
    </Layout>
  );
};
